{
  "name": "worker",
  "version": "1.0.0",
  "scripts": {
    "start": "npx playwright-mcp server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "ioredis": "^5.6.1"
  }
}
