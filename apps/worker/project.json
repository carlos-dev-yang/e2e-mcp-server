{
  "name": "worker",
  "root": "apps/worker",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Build command for worker (to be defined)'"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Test command for worker (to be defined)'"
      }
    }
  }
}